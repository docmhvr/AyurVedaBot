rules:
  - name: ensure_relevance
    trigger: user intent is off-topic
    action:
      - respond "I'm best suited to answer questions about Ayurveda and Natural treatments for health issues."
      - execute fallback_response
